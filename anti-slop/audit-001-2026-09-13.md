# antislop audit 001, 2026-09-13

Scope: every piece of text the project writes (UI strings, step narration, code comments, README, SPEC.md, CLAUDE.md) plus the course references in `references/` (punctuation only). Skills applied: `antislop:antislop` (core) and `antislop:antislop-copywriting`. Mode: one-time AFTER audit of pre-existing copy; the project runs in DURING mode from here on (SPEC.md §18).

Priority follows the rule tier: Hard Gate = HIGH, Purpose-Gate = MEDIUM, Quality Locks and copy-skill patterns = LOW. In this file the banned character is written as U+2014 so the record itself passes `lint:copy`.

## Findings

| # | Where | Finding | Rule | Priority |
|---|---|---|---|---|
| 1 | `src/topics/bst/operations.ts` (8 strings), `operations.test.ts`, SPEC §10.1 templates | U+2014 in step narration ("Empty spot found, inserting", "already exists, a BST", "Found {key}, this node has", "Node has at most one child, splice", "VISIT {k}, visited so far", "Tree is empty, nothing to visit", "Reached an empty link") | R-02 | HIGH |
| 2 | SPEC §10.2 and §10.4 templates | U+2014 in heap and graph narration templates that the next visualizers would copy | R-02 | HIGH |
| 3 | `PlaybackControls.tsx` | Idle counter rendered as two U+2014 characters around a slash | R-02 | HIGH |
| 4 | `src/topics/bst/canvas.tsx` | Empty state used U+2014 as a connector | R-02 | HIGH |
| 5 | 27 source files (first-line `SPEC.md §X` comments), `scripts/extract-content.mjs`, two inline comments in `VisualizerShell.tsx` | U+2014 in code comments | R-02 | HIGH |
| 6 | `SPEC.md` (58), `CLAUDE.md` (31), `README.md` (1 plus an arrow in a code comment) | U+2014 in doc prose | R-02 | HIGH |
| 7 | `references/Week-*.md` (61 across five files), flowing into `src/topics/*/content.ts` (44) | U+2014 in course text | R-02 | HIGH |
| 8 | `src/pages/HomePage.tsx` | `ArrowRightIcon` on every topic card as decoration | R-08 | MEDIUM |
| 9 | `HomePage.tsx`, `AppSidebar.tsx` | `uppercase tracking-wide` labels with no written reason | R-06 | MEDIUM |
| 10 | `PlaceholderCanvas.tsx` | Student-facing placeholder pointed at "SPEC.md §10.x", an internal document | R-38 | LOW |
| 11 | `OperationBar.tsx` | "coming in a later pass": developer jargon in student-facing UI | R-02 (natural text), R-26 (honest label) | LOW |
| 12 | `src/topics/bst/operations.ts` descend and search strings | Arrow glyph used as a prose connector ("45 < 50 [arrow] go left", "[arrow] HIT") | copywriting skill: machine-made connector | LOW |

Reviewed and kept: HomePage intro paragraph, CodePanel idle prompts, input error messages, aria-labels, variant labels, sidebar footer citation, README body, `index.html` title. No buzzwords (R-16). No generic CTAs (R-15): Go, Randomize, and Reset name their action. No fabricated numbers or claims (R-17, R-36): "N operations" is computed, "Visualization not built yet" is an honest placeholder. En dashes in numeric ranges (`Weeks 10–11`) are range typography and are carved out in SPEC §18. The keyboard hint's arrow glyphs name real keys.

## Fixes applied (all 12, approved by the project owner)

1, 2, 12. SPEC §10.1, §10.2, §10.4 templates rewritten in the house style (SPEC §18), then mirrored in `operations.ts` and the vitest expectations. Examples: "45 < 50, so go left.", "Empty spot found. Inserting 45 here.", "60 matches this node: HIT.", "Reached an empty link: MISS. 99 is not in the tree.", "Found 30. This node has 2 children.", "Visit 80. Visited so far: 20, 40, 45, 50, 60, 70, 80."
3. Idle counter now reads `0 / 0`.
4. Empty state now reads "The tree is empty. Insert a key or press Randomize."
5. 29 comment lines changed from a dash to a colon; the generated `content.ts` header template likewise.
6. Punctuation-only pass over SPEC.md, CLAUDE.md, README.md (period, comma, colon, or parentheses chosen per sentence). No wording changed.
7. Punctuation-only pass over all five `references/Week-*.md` files, then `node scripts/extract-content.mjs`. Verified with `git diff --word-diff=porcelain references/`: every added token is an existing word with new punctuation attached; the removed-token set is the same words plus U+2014. No word added or removed.
8. Arrow icon removed; the card is the link and keeps its hover state.
9. `uppercase tracking-wide` removed from the week-group headers and the sidebar eyebrow; both stay small and muted.
10. Placeholder now reads "The {title} visualization is not built yet. The course material for this week is complete." The spec pointer moved to a code comment in each stub `canvas.tsx`.
11. Stub note now reads "Operations for this topic are not built yet."

Also added: SPEC §18 (the standard), SPEC §11 amendment (references are punctuation-editable only), CLAUDE.md mandate section and antislop pointer block, `scripts/check-copy.mjs` wired into `npm run lint` and a `check` job in `.github/workflows/deploy.yml`.

## Delivery Gate, Block 1 (Hard Gate)

- R-02 PASS: `npm run lint:copy` reports zero U+2014 and zero spaced double hyphens across `src/`, `scripts/`, `references/`, `anti-slop/`, README, SPEC, CLAUDE, `index.html`. Playwright `innerText` check on `/`, `/topic/bst` (both tabs), and the three stub pages: no U+2014 in the rendered DOM.
- R-03 PASS: 400px viewport, `document.documentElement.scrollWidth === 400` on `/topic/bst`.
- R-17, R-18, R-36, R-38 PASS: no statistics, testimonials, or claims exist in the UI; the only number shown ("4 operations") is computed from the registry; placeholders are labeled as not built.
- R-24, R-26 PASS: sidebar and home cards link only to registered topics; every button has a handler (Go runs the operation, Randomize and Reset mutate state, playback buttons drive `usePlayback`, tabs toggle content).
- R-27 PASS: empty state (empty tree message), idle state (`0 / 0`, "Press Go to run Insert."), error state (input validation message with `role="alert"`). There is no loading state because nothing is fetched.
- R-32 PASS: Space, ArrowLeft, ArrowRight drive playback (verified in the Playwright run); all controls are native buttons or Radix primitives with focus rings from the theme.
- R-34 PASS: light and dark both use the SPEC §5 tokens; canvases use `var(--color-*)`.
- R-35 PASS: `npm run lint`, `npm test` (14 passing), `npm run build` clean; click-through recorded above (Insert 45, Insert 50 duplicate, Search 60 hit, Search 99 miss, Delete 30 two-child case, Inorder, tab switch, stub pages), zero console errors.
- R-25, R-28, R-33, R-37: not applicable to this pass (no contrast changes, no FAQ, no patch scripts, direction unchanged; the extract script generates a file from a source and does not string-replace UI features).

## Copywriting skill checklist

- No fabricated numbers, testimonials, names, dates, or claims: PASS (see R-17 line above).
- Buzzwords replaced with specific language: PASS, none were present; scan of all UI strings and docs found no R-16 or empty-vocabulary terms.
- No em dashes in the output: PASS (`lint:copy`).
- No excessive quotation marks: PASS; quotes remain only in the course references where they quote the primary source.
- No all-caps emphasis: PASS; the two uppercase labels were the only case and are now sentence case.
- Every sentence names its actor: PASS; narration is imperative or names the key or node ("Mark it visited", "45 < 50, so go left").
- CTAs specific to the action: PASS (Go, Randomize, Reset, Step forward, Step backward, Play, Pause).
- No AI-rhythm tells: PASS; narration is one or two plain sentences per step.
- Voice present: PASS; the course voice in `references/` is untouched apart from punctuation, and the UI copy is plain instructional prose in the same register.
- Read aloud: PASS; the Playwright transcript above reads as a person narrating the algorithm.
